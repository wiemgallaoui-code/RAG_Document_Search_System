Sprint review — Team Atlas — 2026-05-09

Attendees: backend, frontend, QA

Done this sprint:
- Document upload API returns 201 with file id
- Search endpoint returns ranked chunks with similarity scores
- Basic rate limiting on /search (100 requests per minute per IP)

Blockers:
- Embedding model download is slow on first deploy; need caching in Docker image
- German PDFs with umlauts sometimes break the tokenizer — preprocessing ticket opened

Next sprint goals:
- Add pagination to search results
- Write integration tests for empty query and very long query strings
- Update README with example curl commands for recruiters

Action items: Maria documents API in OpenAPI; Jonas profiles memory usage on 10k documents.
